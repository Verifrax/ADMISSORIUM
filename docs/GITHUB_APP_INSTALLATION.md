# GitHub App Installation

Use minimized permissions. Never grant administration write, contents write, secrets write, variables write, deployments write, packages write, or members write by default.
